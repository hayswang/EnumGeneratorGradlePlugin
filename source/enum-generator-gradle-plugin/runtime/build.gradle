apply plugin: 'java'

dependencies {
	compile dep('com.fasterxml.jackson.core:jackson-databind')
	testCompile dep('junit:junit')
}


publishing {
	publications {
		mavenJava(MavenPublication) {
			artifactId archivesBaseName
			from components.java
		}
	}
}